rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // During development: allow all reads, block direct writes from client
    // Change this after going live based on your security needs
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}

